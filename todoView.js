class TodoView {
    static viewHelp(arr) {
        for (let i = 0; i < arr.length; i++) {
            console.log(arr[i])
        }
    }
    static viewList(arr) {
        for (let i = 0; i < arr.length; i++) {
            console.log(arr[i])
        }
    }
    static view(result){
        console.log(result)
    }
}

module.exports = TodoView